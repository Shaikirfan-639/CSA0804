##check if a given key exists in a given dictionary

my_dict = {
    'a': 10,
    'b': 20,
    'c': 30
}

key_to_check = 'b'

if key_to_check in my_dict:
    print(f"Key '{key_to_check}' exists in the dictionary.")
else:
    print(f"Key '{key_to_check}' does not exist in the dictionary.")
